'''
    Multiplication Table generator.

    This code generates the multiplication table.
    - User input of int type > 0 expected
    - No input validation is performed.

'''

while (True):
    
    num=int(input("Which multiplication table you want to generate?:"))
    multiplier=int(input("How long will be your table?:"))
    
    iteration=1
    
    while iteration<=multiplier:  
        print("{} X {} = {}\n".format(num,iteration,num*iteration));  
        iteration = iteration+1;
        
    choice = int(input("Continue printing the table (0=No, 1=Yes?: "))  
    if choice == 0:  
        break;
    

 
